name: tools
dependencies:
  - stage: golang
  - stage: argp-standalone
finalize:
  - from: /toolchain
    to: /toolchain
variant: scratch
shell: /toolchain/bin/bash
steps:
  - prepare: |
      cp /pkg/files/adjust.sh ${TOOLCHAIN}/bin
